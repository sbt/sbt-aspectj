Annotation-based Aspect Weaving Sample
======================================

A simple sample project that weaves its own classes. The aspects are written using annotations.

To run the sample call `sbt run`.
